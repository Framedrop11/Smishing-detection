// to run the backend

cd backend 
python main.py

// to run the app 

open android studio
open emulator and open a device
run the ApiService.java file
use the messaging service built into the emulator to send the virtual device a text like "Urgent Claim your prize! bit.ly/123"
check the notifications for warning.